/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 */
"use strict";

var findout = require('./findout'),
    pkg = require('../package.json');

var lib = findout.bind(this);
lib.version = pkg.version;
module.exports = lib;
